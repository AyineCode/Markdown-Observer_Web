# Markdown Observer · 示例文档

这是一篇用来检查渲染效果的自测文档。下面出现的每一种元素，样式都与 DeepSeek Harness 聊天里的正文一致——因为样式表就是从那里搬过来的。

站内跳转演示：[跳到「代码块」一节](#代码块) · [打开另一篇文档](DEVELOPING.md) · [跳到「数学公式」一节](#数学公式)。点完之后右上角会出现「回到原处」，地址栏也会跟着变，可以直接把某一节的链接发给别人。

## 文字与行内元素

普通段落。**加粗**、*斜体*、***又粗又斜***、~~删除线~~、`行内代码`、以及[一个外部链接](https://api-docs.deepseek.com/)。中英文混排也应该看着舒服：The quick brown fox 跳过那只懒狗，标点、括号（像这样）与数字 12345 都要对齐。

> 引用块。dsh 的引用只有左边一条 2px 的竖线，没有背景色——克制，但一眼能看出层级。
>
> 第二段引用，用来看看多段引用的间距。

---

## 列表

- 无序列表第一项
- 第二项，里面有一小段`代码`
  - 嵌套一层
  - 再嵌套一层
- 第三项

1. 有序列表
2. 第二项
   1. 嵌套的有序列表（dsh 里编号会在行内）
   2. 第二项

- [x] 已经完成的任务
- [ ] 还没做的任务
- [ ] 第三个

## 表格

| 语言 | 用途 | 类型系统 |
|:-----|:-----|--------:|
| C++ | 系统与性能 | 静态、编译期 |
| C# | 应用与 Unity | 静态、运行时也保留 |
| Lua | 嵌入脚本 | 动态 |
| TypeScript | Web 与插件 | 静态、编译后擦除 |

表格在窄屏上会横向滚动，单元格有最大宽度——和 dsh 的处理一样。

## 代码块

```ts
// 代码块有横幅（显示语言）+ 右上角复制按钮，圆角 12px
interface ReadingSettings {
  scale: number      // 字号倍数，1 = dsh 原样
  width: number      // 阅读栏宽，748 就是 dsh 聊天正文的宽度
}

export function apply(settings: ReadingSettings): void {
  document.documentElement.style.setProperty('--read-scale', String(settings.scale));
}
```

### 三级标题也会出现在目录里

三级以下的标题默认在目录里折叠起来，点标题左边的三角展开——目录再长也不会把侧栏撑爆。

```python
def fib(n: int) -> int:
    """生成器也可以，这里演示注释与字符串的配色。"""
    a, b = 0, 1
    for _ in range(n):
        yield a
        a, b = b, a + b
```

```bash
# 打开服务模式：左侧会出现文件树
node serve.mjs ~/notes
```

```json
{ "mode": "server", "root": "/home/ayine/notes", "files": 12 }
```

```
没有标注语言的代码块：不显示语言名，也不上色，但字体与底色一致。
```

## 数学公式

行内公式：当 $a \ne 0$ 时，方程 $ax^2 + bx + c = 0$ 的两个根为 $x = \frac{-b \pm \sqrt{b^2-4ac}}{2a}$。

独立成行的公式：

$$
\int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}
$$

## 脚注与图片

这里有一个脚注引用[^why]，用来检查脚注的编号与末尾区块。

[^why]: 脚注正文。dsh 的脚注只做编号，不做站内跳转链接——这里保持一致。

![这是一张内嵌的 SVG 示意图](data:image/svg+xml;utf8,%3Csvg%20xmlns%3D%27http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%27%20viewBox%3D%270%200%20240%20120%27%3E%3Crect%20width%3D%27240%27%20height%3D%27120%27%20rx%3D%2712%27%20fill%3D%27%235686fe%27%2F%3E%3Ctext%20x%3D%27120%27%20y%3D%2766%27%20font-size%3D%2716%27%20fill%3D%27white%27%20text-anchor%3D%27middle%27%3E%E7%82%B9%E5%87%BB%E7%9C%8B%E5%A4%A7%E5%9B%BE%3C%2Ftext%3E%3C%2Fsvg%3E)

图片点击后会开一个灯箱（Esc 或再点一下关闭）。

## 长段落

把字号调大、栏宽收窄，长文读起来会更舒服；反过来，想看代码多的文档就把栏宽拉宽、字号调小。背景图片会被自动模糊并压暗，正文放在一块半透明磨砂面板里——无论背景多花，正文的对比度都由遮罩和面板两层保证。这些都能在右上角的设置里调，设置会记在浏览器本地，下次打开还在。

> 提示：按 `/` 搜索，按 `T` 切换深浅色，按 `\` 收起侧栏，按 `Esc` 回到普通状态。
